# The local demo requires no API keys or environment secrets.
# Apply D1 migrations: npm run db:local
# Production database binding is managed by Sites.
# Never put MCP tokens or helper invitation links in this file or in Git.
